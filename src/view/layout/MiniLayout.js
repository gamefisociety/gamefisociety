import { Outlet } from "react-router-dom";

// ==============================|| MINIMAL LAYOUT ||============================== //

const MiniLayout = () => (
  <>
    <Outlet />
  </>
);

export default MiniLayout;
